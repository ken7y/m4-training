# M4 Training Requirements - A6000 Optimized
# Install with: pip install -r requirements.txt

# Core ML
transformers==4.40.2
datasets==2.19.1
torch==2.3.0              # CUDA 12.1+ for A6000, includes bf16 support
accelerate==0.30.1

# Evaluation
scikit-learn==1.3.1
scipy>=1.11.0
numpy<2.0.0

# Logging (optional but recommended)
wandb==0.15.1

# Optional: LoRA/PEFT (not used in base training but available)
peft==0.10.0

# Progress bars
tqdm>=4.65.0
